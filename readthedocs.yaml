 version: 2

 build:
   os: ubuntu-lts-latest
   tools:
     rust: latest
   jobs:
     install:
       - cargo install mdbook
     build:
       html:
         # For an example book..
         # - mdbook init docs
         - /home/docs/.asdf/installs/rust/1.91.1/bin/mdbook build docs --dest-dir $READTHEDOCS_OUTPUT/html
